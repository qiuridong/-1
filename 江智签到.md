# 江智签到进度同步

本文档用于同步 `E:\江智签到` 当前脚本状态、常用命令和 GitHub 仓库操作方式，避免后续会话重复分析。

## 1. 当前结论

- 当前真实可用链路不是旧的 `/portal-api/app/index/login` 直登。
- 根据 HAR 和实测，平台现在采用统一认证登录，最终仍通过：
  - `app_user_id -> /portal-api/app/index/checkAppUserIdNew`
  - 换取实际用于签到的 Bearer token
- 脚本已经改成优先复用缓存 token；token 失效时，再重新走绑定和换 token。
- 绑定流程已经小白化：
  1. 先填写签到地址
  2. 自动解析并保存 GCJ-02 经纬度
  3. 填写默认图、早图、晚图
  4. 最后打开浏览器完成统一认证和验证码

## 2. 已实现能力

- 支持 `--bind-account` 浏览器授权绑定账号
- Playwright 不可用时，自动回退 Selenium + 系统 Edge/Chrome
- 自动保存地址、经纬度、默认图、早图、晚图
- 自动保存 `app_user_id / user_id / autonomy_id / token`
- 上传图片后轮询图片 URL，确认可访问再提交签到
- 写接口返回 `401 / 登录信息已过期` 时，自动重新换 token 并重试一次
- 多账号支持
- 晚上补签规则：
  - 如果当前是晚上，并且当天只有 `1` 条签到记录，则允许补签到第 `2` 次
  - 防止首次启用脚本时已经到晚上，只签到过一次却被误判为“晚上已签到”
- 常驻晚间双签规则：
  - 常驻模式触发晚上任务时，如果当天记录为 `0` 条，会先用早图补一次，再等待 `10` 秒用晚图签到一次
  - 可用 `--run-scheduled-slot-now evening` 立即执行同款逻辑，方便当天测试
- 已修复账号串号问题：
  - 重新绑定账号时会清空旧 `token / user_id / nick_name / autonomy_id`
  - 每次拿到 token 后都会重新校验当前账号的实习计划，避免新账号 token 搭配旧账号 `autonomy_id`

## 3. 关键文件

- 主脚本：[auto_checkin.py](E:\江智签到\auto_checkin.py)
- 测试：[test_auto_checkin.py](E:\江智签到\test_auto_checkin.py)
- 配置模板：[checkin_config.example.json](E:\江智签到\repo\checkin_config.example.json)
- 本地配置：[checkin_config.json](E:\江智签到\checkin_config.json)
- token 缓存：[token.json](E:\江智签到\token.json)
- 运行日志：[checkin.log](E:\江智签到\checkin.log)
- HAR 文件：`E:\雷电\sxsx.jxeduyun.com_2026_04_23_16_43_00.har`
- GitHub 仓库工作目录：[repo](E:\江智签到\repo)
- GitHub 仓库地址：[https://github.com/qiuridong/-1.git](https://github.com/qiuridong/-1.git)

## 4. 当前已验证结果

- `python -m pytest E:\江智签到\test_auto_checkin.py -q`
  - `34 passed`
- `python E:\江智签到\auto_checkin.py --bind-account`
  - 已实测能完成浏览器授权绑定
- `python E:\江智签到\auto_checkin.py --once --dry-run`
  - 已实测能完成 token 校验和当日记录查询
- `python E:\江智签到\auto_checkin.py --once`
  - 已实测跑通真实签到链路

## 5. 常用运行命令

### 5.1 Windows EXE 版

当前 `win` 分支提供打包好的 Windows 可执行文件：

- 下载地址：[jiangzhi-checkin.exe](https://github.com/qiuridong/-1/raw/win/dist/jiangzhi-checkin.exe)
- 配置模板下载：[checkin_config.example.json](https://raw.githubusercontent.com/qiuridong/-1/win/checkin_config.example.json)
- 本地构建产物：[jiangzhi-checkin.exe](E:\江智签到\dist\jiangzhi-checkin.exe)

EXE 版不需要安装 Python，也不需要执行 `python -m playwright install`。它默认使用系统 Edge/Chrome 完成浏览器授权。

当前版本已支持高德地址解析，仓库里只提供空配置项：

```json
{
  "amap_key": ""
}
```

说明：

- GitHub 上只保留空的 `amap_key`，不会提交真实 Key
- 你需要在自己机器上的 `checkin_config.json` 里填写真实 `amap_key`
- 如果 `amap_key` 为空，脚本会回退到旧的 Nominatim 地址解析；该服务在国内服务器上通常不稳定
- 部署 EXE 时，建议把 `jiangzhi-checkin.exe`、`checkin_config.json`、`token.json` 放在同一目录
下面示例假设已经进入 EXE 所在目录：

```powershell
cd /d E:\江智签到\dist
```

如果还没有 `checkin_config.json`，先任选一种方式：

```powershell
Copy-Item .\checkin_config.example.json .\checkin_config.json
```

或：

```powershell
.\jiangzhi-checkin.exe --init-config
```

建议至少先填写这些字段：

- `amap_key`
- `clock_address`
- `proof_image_path`
- `proof_images.morning`
- `proof_images.evening`
首次绑定或重新绑定：

```powershell
.\jiangzhi-checkin.exe --config ..\checkin_config.json --bind-account
```

只测试链路，不真正提交：

```powershell
.\jiangzhi-checkin.exe --config ..\checkin_config.json --once --dry-run
```

真实签到一次：

```powershell
.\jiangzhi-checkin.exe --config ..\checkin_config.json --once
```

立即执行常驻模式同款晚上任务：

```powershell
.\jiangzhi-checkin.exe --config ..\checkin_config.json --run-scheduled-slot-now evening
```

启动常驻定时模式：

```powershell
.\jiangzhi-checkin.exe --config ..\checkin_config.json
```

### 5.2 Python 版

首次绑定或重新绑定：

```powershell
python E:\江智签到\auto_checkin.py --bind-account
```

指定账户绑定：

```powershell
python E:\江智签到\auto_checkin.py --bind-account --account 账户名
```

多账户绑定流程（无需手动改 `checkin_config.json`）：

1. 先用 `--bind-account` 绑定第一个账户（写到顶层默认位置）
2. 再用 `--bind-account --account 第二个账户名` 绑定第二个账户。脚本会自动把顶层那次绑定迁移到 `accounts[0]`，并把新账户追加为 `accounts[1]`，二者各自保留独立的 `app_user_id / token / 地址 / 经纬度 / 图片`，互不串号
3. 后续再加更多账户继续 `--bind-account --account 账户名` 即可
4. 已存在的账户用同名 `--bind-account --account 名字` 会进入重新绑定，只覆盖该账户自己的字段

### ⚠️ 关于 `--account 账户名` 里这个"账户名"是什么

**这是你自己起的本地标签，不是任何真实账号信息。**

- **不是** 平台登录账号 / 学号 / 手机号
- **不是** 学校账号密码里的用户名
- **不是** 你在江智 App 里看到的真实姓名 / 昵称
- **就是** 你随手起的一个英文小写短标签，用来在本地区分"这是谁的配置"

**正确示例**：

```powershell
python E:\江智签到\auto_checkin.py --bind-account --account alice
python E:\江智签到\auto_checkin.py --bind-account --account bob
python E:\江智签到\auto_checkin.py --bind-account --account roommate-1
```

**误区示例**（这样写不会出错，但是没有任何"绑定到那个真实账户"的效果，纯粹是给本地条目起了个奇怪的名字）：

```powershell
# ❌ 不要把它当成"我要用学号 2023010001 的账号登录"，它就是个标签
python E:\江智签到\auto_checkin.py --bind-account --account 2023010001

# ❌ 中文能用但会让 token.<账户名>.json 文件名变难读，不推荐
python E:\江智签到\auto_checkin.py --bind-account --account 张三
```

**真实账户身份从哪来**：跑 `--bind-account` 时浏览器会被打开，让你完成**江西教育云的统一认证登录**（输入手机号 + 验证码 / 账号 + 密码 + 点选验证码）。登录成功后脚本从平台拿到 `app_user_id / user_name / nick_name`，**自动**写进 `checkin_config.json` 对应的 `accounts[i]` 里。**这一步才是真正"决定这个本地标签绑定到哪个江智账户"的环节**，不是 `--account 后面写啥`决定的。

**这个标签的作用**：

1. 在 [checkin_config.json](e:/江智签到/checkin_config.json) 的 `accounts[i].name` 里标记这条配置
2. 给该账户生成独立的 token 缓存文件 `token.<账户名>.json`，不同账户的 token 不会互相覆盖
3. 后续命令通过 `--account 同一个标签` 精确指向同一份配置，例如：
   ```powershell
   python E:\江智签到\auto_checkin.py --once --account alice
   python E:\江智签到\auto_checkin.py --once --account bob
   ```

**取名建议**：

- 用英文小写短词或加连字符：`alice` / `bob` / `student-1` / `me-school`
- **避免**：中文（token 文件名会变 `token.--.json`）、空格、引号、斜杠等特殊字符
- **不要重名**：一份配置里两条 `name` 必须唯一
- **不要叫 `default`**：脚本内部用它表示"顶层那个无名账户"，自己新建账户撞这个名字会乱

常驻模式启动后会按每个账户分别注册早晚两个 slot 调度，每个账户的 token 缓存写在 `token.<账户名>.json`。

只执行一次真实签到：

```powershell
python E:\江智签到\auto_checkin.py --once
```

只测试链路，不真正提交：

```powershell
python E:\江智签到\auto_checkin.py --once --dry-run
```

强制签到一次，不走“已签到跳过”判断：

```powershell
python E:\江智签到\auto_checkin.py --once --force
```

只跑早上时段：

```powershell
python E:\江智签到\auto_checkin.py --once --slot morning
```

只跑晚上时段：

```powershell
python E:\江智签到\auto_checkin.py --once --slot evening
```

立即执行一次常驻模式同款早上任务：

```powershell
python E:\江智签到\auto_checkin.py --run-scheduled-slot-now morning
```

立即执行一次常驻模式同款晚上任务：

```powershell
python E:\江智签到\auto_checkin.py --run-scheduled-slot-now evening
```

说明：如果当天记录为 `0` 条，晚上同款任务会自动先早图、等 `10` 秒、再晚图连签两次。

指定账户执行一次：

```powershell
python E:\江智签到\auto_checkin.py --once --account 账户名
```

指定账户做 dry-run：

```powershell
python E:\江智签到\auto_checkin.py --once --dry-run --account 账户名
```

启动常驻定时模式：

```powershell
python E:\江智签到\auto_checkin.py
```

查看帮助：

```powershell
python E:\江智签到\auto_checkin.py --help
```

## 6. GitHub 仓库同步命令

当前真正的 git 仓库目录是：

```powershell
E:\江智签到\repo
```

查看状态：

```powershell
git -C E:\江智签到\repo status
```

同步主脚本、测试、README 和本说明文档：

```powershell
Copy-Item E:\江智签到\auto_checkin.py E:\江智签到\repo\auto_checkin.py -Force
Copy-Item E:\江智签到\test_auto_checkin.py E:\江智签到\repo\test_auto_checkin.py -Force
Copy-Item E:\江智签到\江智签到.md E:\江智签到\repo\江智签到.md -Force
```

进入仓库后提交：

```powershell
git -C E:\江智签到\repo add auto_checkin.py test_auto_checkin.py README.md 江智签到.md
git -C E:\江智签到\repo commit -m "Update bind flow and command docs"
git -C E:\江智签到\repo push origin main
```

提交前隐私检查：

```powershell
Select-String -Path E:\江智签到\repo\auto_checkin.py,E:\江智签到\repo\README.md,E:\江智签到\repo\江智签到.md -Pattern "JWT_TOKEN|PHONE_NUMBER|PASSWORD|J:\\个人路径|微信图片"
```

GitHub 仓库只提交脱敏源码和说明文档，不提交：

- `checkin_config.json`
- `token.json`
- `checkin.log`
- `__pycache__/`
- `.pytest_cache/`

## 7. 现阶段真实使用建议

- 平时直接运行：

```powershell
python E:\江智签到\auto_checkin.py
```

- 如果只想临时手动跑一次：

```powershell
python E:\江智签到\auto_checkin.py --once
```

- 如果 token 可用，脚本会优先直接使用 token。
- 如果 token 失效，脚本会自动重新进入绑定流程，重新让用户填写地址、图片并打开浏览器授权。

## 8. 一句话状态

当前脚本已经从“依赖手动抓 token”推进到“浏览器统一认证绑定一次，之后自动换 token 并自动签到”的状态，核心功能和文档都已转到 GitHub 仓库维护。
